import { Component } from '@angular/core';

@Component({
    selector: "app-name",
    template: `
        <input type="text">
        <input type="text">
        <button type="button">Show!</button>
    `
})
export class NameComponent {

}